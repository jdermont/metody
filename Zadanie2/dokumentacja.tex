%& --translate-file=cp1250pl
\documentclass[12pt,a4paper]{article}
\usepackage[left=1.25in,right=1.25in,top=1.25in,bottom=1.25in,dvips]{geometry}


\usepackage{polski}
\usepackage[OT4]{fontenc}
\usepackage{amsthm,amsmath,amsfonts,amssymb}

\usepackage{verbatim}

\usepackage{color}
\usepackage{alltt}
\usepackage{enumerate}
\usepackage{url}
\usepackage{listings}
\lstset{language=C++,
tabsize=1,
basicstyle=\ttfamily\footnotesize,
frame=shadowbox,
breaklines=true,
showstringspaces=false}

\author{Marcin Horoszko, Rados³aw G³ombiowski, Jacek Dermont}
\title{Dokumentacja do zadania 2.5}

\newtheorem{tw}{Czêœæ}[section]
\theoremstyle{definition}
\newtheorem{defi}[tw]{}

\begin{document}

\maketitle

\section{Zadanie 2.5}
Dla równania $f(x)$ = $0$, gdzie $f(x)$ = $lnx + x - 5$, wczytaæ a,b $\in$ $R$ takie, by 0 $<$ a $<$ b oraz $f(a)\cdot f(b)$ $<$ 0. Nastêpnie,
dopóki ``u¿ytkownik siê nie znudzi'', wczytywaæ wartoœæ 0 $<$ $\epsilon$ $<$ 1 i metod¹ po³owienia na $[$a,b$]$ przybli¿yæ z dok³adnoœci¹ $\epsilon$
rozwi¹zanie tego równania. Rozwi¹zanie to przybli¿yæ równie¿ metod¹ Newtona z $x_0$ = a, przy czym $x_k$ bêdzie dobrym przybli¿eniem, gdy 
$|x_k-x_{k-1}|\leqslant\epsilon$. Porównaæ iloœæ kroków wykonanych metod¹ po³owienia i metod¹ Newtona.

\section{Podstawowe pojêcia}
\begin{defi}[Metoda po³owienia (równego podzia³u, bisekcji)]
Jedna z metod rozwi¹zywania równañ nieliniowych. Opiera siê ona na twierdzeniu Bolzano-Cauchy'ego: \emph{Je¿eli funkcja ci¹g³a $f(x)$ ma na koñcach przedzia³u
domkniêtego wartoœci ró¿nych znaków, to wewn¹trz tego przedzia³u, istnieje co najmniej jeden pierwiastek równania $f(x)=0$.}. Przebieg metody: \\
Wybieramy przybli¿enie $\epsilon$ (np. $\epsilon$ = 0.001). \\
1. Sprawdzamy czy dla $c=\frac{a+b}{2}$ $f(c)=0$. Jeœli tak, to koñczymy, a $c$ jest pierwiastkiem funkcji. \\
2. Je¿eli nie, dzielimy przedzia³ na po³owy $[a,c] [c,b]$. Je¿eli $f(a)\cdot f(c)$ $<$ 0 to $b = c$, w przeciwnym przypadku $a = c$. \\
3. Powtarzamy proces, dopóki odleg³oœæ miêdzy $a$ i $b$ nie bêdzie mniejsza lub równa przybli¿eniu $\epsilon$. Zarówno a i b bêd¹ pierwiastkami spe³niaj¹cymi
zadane przybli¿enie.
\end{defi}
\begin{defi}[Metoda Newtona]
Iteracyjny algorytm wyznaczania przybli¿onej wartoœci pierwiastka funkcji. Mo¿na j¹ zastosowaæ, gdy w przedziale $[a,b]$ funkcja jest ci¹g³a, $f(a)\cdot f(b)$ $<$ 0
oraz pierwsza i druga pochodna funkcji maj¹ sta³y znak na tym przedziale. Przebieg metody: \\
Wybieramy przybli¿enie $\epsilon$ (np. $\epsilon$ = 0.001) oraz wyliczamy pochodn¹ $f'(x)$. \\
1. $x_0$ = $a$ \\
2. Kolejny k-ty x wyliczamy ze wzoru: $x_{k+1}$ = $x_k$-$\frac{f(x_k)}{f'(x_k)}$. \\
3. Powtarzamy 2. punkt dopóki $|x_{k+1} - x_k| \leqslant \epsilon$. $x_{k+1}$ jest szukanym pierwiastkiem spe³niaj¹cym zadane przybli¿enie. \\
Przy spe³nionych za³o¿eniach b³¹d przybli¿enia maleje kwadratowo. Jednak¿e, jeœli punkt startowy jest daleko od pierwiastka, metoda Newtona mo¿e byæ rozbie¿na i w
konsekwencji dzia³aæ w ``nieskoñczonoœæ''. W tym wypadku nale¿y siê zabepieczyæ, np. poprzez ustalenie maksymalnej iloœci iteracji.
\end{defi}

\section{Metoda numeryczna}
Posiadaj¹c a, b (0$<$a$<$b i $f(a)\cdot f(b)$ $<$0) oraz $\epsilon$ (0$<\epsilon<$1) mo¿emy przybli¿yæ wartoœæ pierwiastka $f(x)$ = $lnx + x - 5$ powy¿szymi
metodami.\\
Dla metody po³owienia$^*$:
\begin{itemize}
\item $i$ = 0 (iloœæ kroków)
\item tworzymy pêtlê \textbf{while}, której warunkiem dzia³ania jest $|a-b|>\epsilon$:
\begin{itemize}
\item $i++$
\item c = $\frac{a+b}{2}$
\item jeœli $f(a)\cdot f(c)$ $<$ $0$, to b = c
\item w przeciwnym przypadku a = c
\end{itemize}
\item zmienna $c$ to zadane przybli¿enie
\item zmienna $i$ to iloœæ wykonanych kroków
\end{itemize}
\small $^*$ poniewa¿ dla równania w zadaniu $x$ jest niewymierne, omijamy sprawdzanie $f(c) == 0$ \\ \\
\normalsize Dla metody Newtona:
\begin{itemize}
\item definiujemy pochodn¹ $f'(x)$ = $\frac{1}{x}+1$
\item $j$ = 0 (iloœæ kroków)
\item $x_0$ = a
\item tworzymy nie koñcz¹c¹ siê pêtlê \textbf{while}:
\begin{itemize}
\item $j++$
\item $x_1$ = $x_0-\frac{f(x_0)}{f'(x_0)}$
\item jeœli $|x_0-x_1|\leqslant\epsilon$, to przerywamy \textbf{pêtlê}
\item jeœli $j == 100$, to przerywamy \textbf{pêtlê}
\item $x_0$ = $x_1$
\end{itemize}
\item jeœli $j < 100$, zmienna $x_1$ jest naszym szukanym przybli¿eniem
\item jeœli $j == 100$, metoda Newtona jest w tym przypadku rozbie¿na; nie otrzymaliœmy za³o¿onego przybli¿enia
\item zmienna $j$ to iloœæ wykonanych kroków
\end{itemize}

\section{Opis programu}
Program zosta³ w ca³oœci napisany w jêzyku $C.$
\\\\
Wejœcie
\begin{itemize}
\item $a$, liczba typu double, taka ¿e $a>0$; jeœli $a\leqslant 0$, to program zapyta ponownie
\item $b$, liczba typu double, taka ¿e $b>a$; jeœli $b\leqslant a$, to program zapyta ponownie \
jeœli $f(a)\cdot f(b)$ $<$ 0, to program ponownie zapyta o $a$ i $b$
\item $\epsilon$, liczba typu double, taka ¿e $0<\epsilon<1$, podawana w nieskoñczonej pêtli dopóki u¿ytkownik ``siê nie znudzi'';
 jeœli $\epsilon$ jest spoza przedzia³u $[0,1]$, program zapyta ponownie
\end{itemize}
\bigskip
Wyjœcie
\begin{itemize}
\item $c$, liczba typu double, pierwiastek funkcji o przybli¿eniu $\epsilon$
\item $i$, liczba ca³kowita, iloœæ kroków wykonanych za pomoc¹ metody po³owieñ
\item $x_1$, liczba typu double, pierwiastek funkcji o przybli¿eniu $\epsilon$ $^*$
\item $j$, liczba ca³kowita, iloœæ kroków wykonanych za pomoc¹ metody Newtona $^*$
\end{itemize}
\small $^*$ jeœli $j == 100$ to $x_1$ nie jest pierwiastkiem, bo metoda Newtona sta³a siê rozbie¿na \ \\ \\
\normalsize
Program sk³ada siê tylko z jednego pliku $main.cpp$. Zawiera wszystkie potrzebne funkcje.
\end{document}